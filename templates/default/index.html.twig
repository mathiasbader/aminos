{% extends 'base.html.twig' %}

{% block body %}

    <div class="dashboard">
        <a href="{{ url('overview', { 'param' : ''}) }}" class="btn">Overview</a><br>
        <a href="{{ url('imgToName') }}" class="btn">Image to name</a><br>
        <a href="{{ url('nameToImg') }}" class="btn">Name to image</a><br>
        <a href="{{ url('codeToName') }}" class="btn">Code to name</a><br>
    </div>

    <a href="{{ url('about') }}">About</a>

{% endblock %}
